

const numeros = [ 1,2,3,4,5,8]

numeros.splice ( 1, 1);  // excluir elementos dos arrays - primeiro a possicao e segundo a quantidade de elementos que quero excluir.  

console.log( numeros); 

numeros.pop ( ); // remove o ultimo elemennto do arrays  

console.log( numeros )


 // esses metos faz alteracao no arrays  
