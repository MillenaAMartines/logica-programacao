const Hora = 17; 
const Saudacao = Hora <= 11 ? "Bom Dia": (Hora <= 17 ? "Boa Tarde" : "Boa noite " );  
// se tiver mais 2 condicao; executamos um ternario dentro do outro; 

console.log(Saudacao); 

